<nav class="navbar navbar-expand-lg navbar-dark bg-dark-gray">
  <div class="container">
    <%= link_to 'StoresHub', root_path, class: 'navbar-brand text-yellow fs-2' %>
    
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <% if current_user && current_user.admin? %>
    <%= link_to 'Orders', receipts_path, class: 'nav-link text-offwhite fs-4' %>
    <% end %>
      <% if current_user %>
      <ul class='navbar-nav ml-auto'>
        <li class="nav-item user-name text-yellow">
          <%= fa_icon 'user' %> <%= current_user.full_name %>
        </li>
        <li class="nav-item">
            <%= link_to("Logout", destroy_user_session_path, class: 'btn btn-xs btn-danger logout-button', method: :delete) %>
          </li>
        </ul>
        <% end %>
    </div>
  </div>

</nav>