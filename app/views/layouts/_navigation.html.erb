<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to 'Stores Hub', root_path, class: 'navbar-brand txt-yellow' %>
    
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <% if current_user && current_user.admin %>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-expanded="false">
              Users
            </a>
            <div class="dropdown-menu">
              <%= link_to 'Manage users', users_path, class:'dropdown-item' %>
              <%= link_to 'Create user', new_user_registration_path, class:'dropdown-item' %>
            </div>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-expanded="false">
              Stores
            </a>
            <div class="dropdown-menu">
              <%= link_to 'Manage stores', stores_path, class:'dropdown-item' %>
              <%= link_to 'Create store', new_store_path, class:'dropdown-item' %>
            </div>
          </li>
        </ul>
      <% end %>
      <% if current_user %>
      <ul class='navbar-nav ml-auto'>
        <li class="nav-item user-name white">
          <%= fa_icon 'user' %> <%= current_user.full_name %>
        </li>
        <li class="nav-item">
            <%= link_to("Logout", destroy_user_session_path, class: 'btn btn-xs btn-danger logout-button', method: :delete) %>
          </li>
        </ul>
        <% end %>
    </div>
  </div>

</nav>